{
  "name": "npm-script",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^9.4.0",
    "browser-sync": "^2.26.3",
    "imagemin-cli": "^4.0.0",
    "node-sass-chokidar": "^1.3.4",
    "parallelshell": "3.0.1",
    "postcss-cli": "^6.0.1",
    "rimraf": "^2.6.2",
    "uglify-js": "^3.4.9"
  },
  "scripts": {
    "//": "Learned from this tutorial",
    "//": "https://css-tricks.com/why-npm-scripts/",
    "//": "-------------- (∩` ﾛ ´)⊃━炎炎炎炎炎 ------------------"
    ,
    "//": "PACKAGE      ____________________________________________________________________________________",
    "clean": "rimraf dist/{css/*,js/*,images/*}",
    "watch-sass": "node-sass-chokidar build/scss/ --source-map true --output build/css/ --watch --error-bell",
    "autoprefixer": "postcss --use autoprefixer --autoprefixer.browsers '> 5%, ie 9' --replace build/css/*",
    "uglify": "mkdir -p dist/js && uglifyjs build/js/*.js -m -o -c dist/js/main.min.js",
    "opt-img": "imagemin build/img dist/img -p",
    "serve": "browser-sync start --server './build' --files './build/*.html, ./build/css/*.css, ./build/js/*.js'"
    ,
    "//": "BUNDLING     ____________________________________________________________________________________",
    "build:css": "yarn run watch-sass && yarn run autoprefixer",
    "dist:image": "yarn run opt-img"
    ,
    "//": "DEVELOPMENT  ____________________________________________________________________________________",
    "dev": "parallelshell 'yarn run serve' 'yarn run build:css'"
    ,
    "//": "PRODUCTION   ____________________________________________________________________________________"
  }
}
